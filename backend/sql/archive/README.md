# Archived SQL Files

These SQL files are historical debug and iteration artifacts from the RPC gateway development process. They are **not for production use**.

The canonical gateway SQL is located at: `../gateway/AGENT_GATEWAY_CANONICAL_FIXED_SD.sql`

See `../../backend/DEPLOY_NOTES.md` and `../../backend/DEBUG_RPC_ISSUE.md` for context on why these iterations were needed.
